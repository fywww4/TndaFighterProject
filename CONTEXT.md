# TndaFighter

一名本機玩家與一名 CPU 對戰的格鬥遊戲。

## Language

**玩家出生點（PlayerStart）**：
指定玩家開場站位的位置。

**CPU 出生點（CpuStart）**：
指定 CPU 開場站位的位置；它是定位點，不負責生成角色。
_Avoid_：CPU Spawner（指稱出生位置時）

**開場定位完成**：
雙方角色已生成，出生碰撞調整已完成，並設定面向彼此的初始朝向；不以角色完成落地為條件。
